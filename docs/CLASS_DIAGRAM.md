# Class Diagram

A visual class diagram for the backend Java components is available in the repository:

- `docs/class-diagram.svg` (SVG image)

This diagram shows the model classes (`User`, `SoilAnalysis`), the Spring Data repositories and the controllers that use them.

Open the `docs/class-diagram.svg` file in your browser or VS Code to view the diagram.